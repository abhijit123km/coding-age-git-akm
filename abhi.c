#include <stdio.h>
int main(){
    int a,b, sum=0;
    printf("enter two numbers:");
    scanf("%d %d", &a,&b);
   // now for addition
     sum = a+b;
   printf("your sum is""%d",sum);
    return 0;
}
